class Solution {
    public static ArrayList<Integer> fillArrayList(int arr[]) {
        // Your code here
        ArrayList<Integer>list = new ArrayList<>();
        for(int i: arr){
            list.add(i);
        }
        return list;
    }
}